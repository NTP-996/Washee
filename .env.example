# washee — env for the /api/book serverless function.
# Production: set these in Vercel → Project → Settings → Environment Variables.
# Local dev:  copy this file to .env and fill it in, then run `vercel dev`.
WASHEE_BOT_TOKEN=
WASHEE_CHAT_ID=
