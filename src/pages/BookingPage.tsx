// Placeholder — the slot picker + booking form arrive in Slice 3.
export default function BookingPage() {
  return (
    <main className="mx-auto min-h-screen max-w-2xl bg-canvas px-5 py-10 text-ink">
      <div className="speed-stripe mb-6 h-1 w-12 rounded-full" />
      <h1 className="text-2xl font-bold tracking-tight">Book a wash</h1>
      <p className="mt-3 text-muted">Slot picker + booking form — arriving in Slice 3.</p>
    </main>
  );
}
