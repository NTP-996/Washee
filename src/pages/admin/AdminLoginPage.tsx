// Placeholder — the admin auth + dashboard land in Slice 4.
export default function AdminLoginPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center bg-canvas px-5 text-ink">
      <div className="w-full max-w-sm rounded-2xl border border-hairline bg-panel p-8">
        <div className="speed-stripe mb-5 h-1 w-12 rounded-full" />
        <h1 className="text-2xl font-bold tracking-tight">Admin</h1>
        <p className="mt-3 text-sm text-muted">Operator sign-in — arriving in Slice 4.</p>
      </div>
    </main>
  );
}
