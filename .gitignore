# dependencies
node_modules

# build output
dist

# local env & Vercel state — never commit. Production secrets live in Vercel env vars;
# .env is only for local dev. Copy .env.example → .env and fill it in.
.env
.env*.local
.vercel

# misc
.DS_Store
